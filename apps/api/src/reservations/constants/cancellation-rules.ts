/**
 * EBA-112: Cancellation rules for participant self-cancel.
 * - Cancel allowed only if event start is at least this many hours in the future.
 */
export const CANCEL_MIN_HOURS_BEFORE_EVENT = 24;
